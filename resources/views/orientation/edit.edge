@layout('layouts/main')

@section('main')

@set('title', 'Orientações')

<div class="card">
  <h1>Editar trabalho de orientação</h1>

  <form action="{{ route('OrientationsController.update', {id: orientation.id}) }}?_method=PUT" method="post">
    @include('orientation/fields')
    <button class="btn">Salvar</button>
  </form>
</div>
@end